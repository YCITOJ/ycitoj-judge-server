#pragma once
#include <iostream>
#include <fstream>
#include <string>
namespace Comparator{
    bool compare(std::string out, std::string ans, std::string &msg);
    bool spj(std::string user_out, std::string prob_in);
}